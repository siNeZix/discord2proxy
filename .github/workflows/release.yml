name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.3'

      - name: Derive version
        id: ver
        shell: bash
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build CLI
        shell: bash
        run: |
          go build -trimpath \
            -ldflags "-s -w -X discord-szx/internal/config.Version=${{ steps.ver.outputs.version }}" \
            -o build/discord2proxy-cli.exe ./cmd/

      - name: Build GUI
        shell: bash
        run: |
          go build -trimpath \
            -ldflags "-s -w -H=windowsgui -X discord-szx/internal/config.Version=${{ steps.ver.outputs.version }}" \
            -o build/discord2proxy-gui.exe ./cmd/gui/

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            build/discord2proxy-cli.exe
            build/discord2proxy-gui.exe
